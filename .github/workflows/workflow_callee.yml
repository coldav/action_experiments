on:
  workflow_call:
    inputs:
      arch_list:
        required: true
        type: string
  workflow_dispatch:
    inputs:
      llvm_version:
        required: true
        type: string
      update_cache:
        required: true
        type: boolean
  # pull_request:
  #   paths:
  #     - '.github/workflows/workflow_callee.yml'

# env:
#   LLVM_VERSION: 17
#   UPDATE_CACHE: false
# jobs:
#     # affected-apps:
#     #   runs-on: ubuntu-latest
#     #   name: A job to say hello
#     #   outputs:
#     #       apps: ${{ steps.step1.outputs.test}}
#     #   steps:
#     #     - uses: actions/checkout@v3
#     #     - id: step1
#     #       run: echo "test=$arch_list" >> "$GITHUB_OUTPUT"
#     example_matrix:
#       runs-on: ubuntu-latest
#       # needs: affected-apps
#       strategy:
#         matrix:
#           app: ${{ fromJson(${{ inputs.arch_list }}) }}
#       steps:
#         - uses: actions/setup-node@v3
#         - run: echo "${{ matrix.app }}"
